#ifndef HELPERS_DEF
#define HELPERS_DEF

void plot_1d(float x0, float dx, int nx, float* y, char* label);
void plot_2d(float x0, float dx, int nx, float y0, float dy, int ny, float* z, char* label);

#endif
